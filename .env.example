# Uso este arquivo como modelo para testes locais.
# A senha real do banco deve ficar apenas no meu .env, que nao vai para o GitHub.

PORT=3000
DATABASE_URL=postgresql://usuario:senha@host/neondb?sslmode=require

# Variaveis antigas de MySQL ficam aqui so se eu quiser testar uma versao local com MySQL.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=portfolio_acessos_luana
